NODE_ENV=development
HOST=127.0.0.1
PORT=3000
WEB_PORT=5173
APP_NAME=HackAlem Starter
# Only non-secret data may use a VITE_ prefix.
# Provider keys are intentionally absent until the contest use case is chosen.
