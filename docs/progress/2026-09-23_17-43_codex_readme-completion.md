# Сверка всех README и подготовка документационного коммита

Дата: 23 сентября 2026 года. Время имени файла — локальное время записи, не конкурсный тайминг.

## Изменения

- Обновлены все восемь README: корневой, docs, implementation, frontend, три языка набора данных и vendored proto.
- Описаны задача Halyk Bank/Saqta, реализованные пути, владельцы файлов, примеры RU/KK/mixed, ошибки и незавершённые функции.
- Добавлены запуск без `.env`, полный список настроек и контрактов OpenAI/Yandex/ElevenLabs, production Node, Docker/Compose, HTTPS/WS и границы доступа.
- По уточнению пользователя: `ELEVENLABS_VOICE_ID` должен быть существующим голосом, загруженным/добавленным в аккаунт `ELEVENLABS_API_KEY` и доступным этому ключу.
- Разделены unit/integration/SSR, built smoke, внешний smoke и платный dev-eval; метрики точности/латентности не придуманы.
- В README датасета добавлены отдельные вступления; исходный текст, JSON, разметка и evaluator сохранены. В proto README сохранены источники и лицензии.
- Требования аутентификации OpenAI сверены с официальной документацией через OpenAI Docs; существующие модели не менялись. Значения локальных секретов не читались.

## Проверки

- `git pull --no-rebase`: Already up to date, база `0c5f22a`; после возврата stash конфликтов нет.
- `pnpm.cmd run verify`: успешно — doctor, Prettier, typecheck, 86/86 тестов, production build, built smoke.
- Использованы временные `RAYON_NUM_THREADS=1` и `NODE_OPTIONS="--max-old-space-size=512 --max-semi-space-size=4"`, как при предыдущей проверке на этой машине.
- `git diff --check`: успешно. Все локальные пути Markdown-ссылок восьми README существуют.

## Границы

### Повторная синхронизация перед push, 17:47

Первый push коммита `fb1911a` отклонён: remote получил три новых коммита до `e99dc83`. Повторный `git pull --no-rebase` вызвал конфликты в `docs/README.md`, `docs/STATUS.md`, `docs/implementation/README.md`, `src/web/README.md`. Они разрешены по содержимому: сохранена новая структура документации, учтены VAD/EOU, ожидание OpenAI final, безопасная TTS-диагностика и последний voice/text результат в правой trace-панели. Upstream-код, тесты и отчёты сохранены.

Проверка объединения сначала дала 95/96: upstream заменил глубокую защиту вложенного voice-result поверхностной. В `realtime/client.ts` восстановлены `deepFreeze(structuredClone(...))`, защищённая копия `lastError` и обработчик playback error из предыдущей интеграции; новая логика VAD/отмены сохранена. Повторный полный `pnpm.cmd run verify` прошёл: 96/96 тестов, типы, форматирование, build и built smoke.

### Что не проверялось

Живые API, микрофон, точность, задержки, Docker-сборка и публичный deployment в этой задаче не проверялись; квота провайдеров не расходовалась. Отдельная пользовательская правка Dockerfile не изменена и не включается в документационный коммит. Резервный stash сохранён.

Следующий продуктовый шаг: отдельная согласованная live-проверка RU/KK/mixed и голоса; mock executor, подтверждения и handoff остаются открытыми. Commit/push фиксируются только по фактическому результату Git, не как заранее выполненные действия.
