# Повторная проверка Docker

Время проверки: 2026-09-23 14:59, Asia/Qyzylorda (09:59 UTC). Проверен текущий `main` на commit `6d8d088` (`origin/main` указывает на тот же commit). Предыдущий запуск Docker не состоялся, когда Linux engine был недоступен; это отдельный успешный повторный запуск.

- `docker build -t hack-b269965b-phase0 .` — успешно. В build-стадии прошли typecheck, 36/36 тестов и Vite build; runtime-образ создан.
- В runtime-контейнере загрузчик прочитал `data/voice-router`: 40 сценариев, 43 допустимых ID, дата симуляции `2026-10-01`.
- Временно запущен HTTP-контейнер с локально опубликованным портом. `pnpm run smoke -- http://127.0.0.1:32768` — успешно: health, echo и API 404.
- Проверочный контейнер остановлен; он был запущен с `--rm`. Образ `hack-b269965b-phase0:latest` сохранён локально.

Проверка подтверждает Docker runtime и исходный каталог. Живые OpenAI/Yandex/ElevenLabs вызовы, голосовой сценарий и внешний deployment этим smoke не проверялись.
