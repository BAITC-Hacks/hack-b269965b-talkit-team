# Сверка и обновление README

Фактическое время: 2026-09-23 16:58:44, Asia/Qyzylorda (UTC+05:00). Исполнитель: Codex. Область: документация.

## Цель

Переработать все активные проектные `README.md` по требованиям Voice Router и фактическому состоянию репозитория, не приписывая незавершённые voice/UI/integration-возможности готовому продукту.

## Изменения

- Корневой `README.md`: добавлены честная матрица готовности, целевой поток, реальная структура, требования, setup/dev/API/Docker-команды, безопасность и известные ограничения.
- `docs/README.md`: обновлены быстрый маршрут чтения, карта документов, источники истины и фактический технический контекст.
- `docs/implementation/README.md`: план фаз синхронизирован с уже реализованной серверной частью и незавершёнными voice/UI/action/eval/deploy-частями.
- `src/web/README.md`: описаны текущий Echo-экран, существующие PCM/WebSocket-модули, их неподключённый статус и требования к будущему Voice Router UI.
- `data/voice-router/README.md` прочитан и проверен, но не изменялся: это исходный англоязычный README набора организатора, который должен сохраняться вместе с исходными JSON. Локализованные `README.ru.md` и `README.kz.md` также не редактировались.
- `docs/STATUS.md` приведён к короткому актуальному состоянию; удалены находившиеся в нём конфликтные маркеры и зафиксирован результат этой проверки.

Параллельные изменения `roman_sol_medium_todo.md`, browser realtime-кода и `VoiceVisualizer.vue` не редактировались и не форматировались этой задачей.

## Проверки

- `git diff --check` — успешно для документационных изменений.
- `node_modules/.bin/prettier --check README.md docs/README.md docs/implementation/README.md src/web/README.md data/voice-router/README.md` — успешно после форматирования четырёх изменённых файлов.
- Проверка относительных Markdown-ссылок четырёх изменённых README скриптом PowerShell — отсутствующих целей не найдено.
- `pnpm.cmd run check` — два запуска успешно; повторный запуск после завершения документации: TypeScript/Vue typecheck и 36/36 тестов.
- Первый `pnpm.cmd run verify` — не дошёл до project scripts: упакованный runtime системного pnpm завершился с `Array buffer allocation failed` под Node 20.11.1.
- Повторный `pnpm.cmd run verify` — doctor прошёл; общий `format:check` остановился на девяти файлах: `docs/STATUS.md`, семи `src/server/features/voice-router/*.ts` и `src/web/components/ui/VoiceVisualizer.vue`. Полный verify не прошёл, последующие check/build/smoke внутри него не запускались.

После первого успешного `check` HEAD продвинулся параллельным commit до `282b285` (`voice steck v2`), а рабочее дерево продолжило меняться в realtime-файлах. Этот отчёт не приписывает себе тот commit. После завершения документации выполнен второй успешный `check` на текущем на тот момент рабочем дереве.

## Ограничения и следующий шаг

- README отражают код, но сквозной голос, live providers, eval и публичный deployment остаются незавершёнными.
- Полный `verify` нужно повторить после стабилизации и согласованного форматирования текущих исходников.
- Следующий продуктовый шаг — закончить Phase 1 web/text vertical slice и его тесты, затем соединить существующий browser transport с реальным server voice pipeline.
